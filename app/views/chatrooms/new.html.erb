<div class="container">
  <div class="row">
    <div class="col-12">
      <%= simple_form_for([@activity, @chatroom]) do |f| %>
        <%= f.input :title %>
        <%= f.input :description %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>